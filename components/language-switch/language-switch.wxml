<view class="switch-shell">
  <block wx:for="{{options}}" wx:key="value">
    <button
      class="switch-item {{value === item.value ? 'active' : ''}}"
      data-value="{{item.value}}"
      bindtap="handleSelect"
    >
      {{item.label}}
    </button>
  </block>
</view>
